# postgresql.auto.conf - Standby
# 由 streaming_replication.py 生成
primary_conninfo = 'host=primary port=5432 user=replicator password=repl_pass application_name=standby1'
primary_slot_name = 'standby_slot'
